import { Settings } from 'lucide-react'
import { PlaceholderPage } from '@/components/ui/PlaceholderPage'

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Configuración"
      description="Usuarios, roles, preferencias del sistema y parámetros generales."
      icon={Settings}
    />
  )
}
